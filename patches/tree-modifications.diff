diff --git a/package/kernel/mac80211/Makefile b/package/kernel/mac80211/Makefile
index 9014ce8..517c269 100644
--- a/package/kernel/mac80211/Makefile
+++ b/package/kernel/mac80211/Makefile
@@ -35,6 +35,7 @@ PKG_CONFIG_DEPENDS:= \
 	CONFIG_PACKAGE_CFG80211_TESTMODE \
 	CONFIG_PACKAGE_MAC80211_DEBUGFS \
 	CONFIG_PACKAGE_MAC80211_MESH \
+	CONFIG_PACKAGE_MAC80211_NSS_SUPPORT \
 	CONFIG_PACKAGE_MAC80211_TRACING \
 	CONFIG_PACKAGE_IWLWIFI_DEBUG \
 	CONFIG_PACKAGE_IWLWIFI_DEBUGFS \
@@ -86,6 +87,7 @@ config-$(CONFIG_PACKAGE_CFG80211_TESTMODE) += NL80211_TESTMODE
 
 config-$(call config_package,mac80211,$(ALL_VARIANTS)) += MAC80211
 config-$(CONFIG_PACKAGE_MAC80211_MESH) += MAC80211_MESH
+config-$(CONFIG_PACKAGE_MAC80211_NSS_SUPPORT) += MAC80211_NSS_SUPPORT
 
 include ath.mk
 include broadcom.mk
@@ -130,7 +132,7 @@ define KernelPackage/mac80211
   $(call KernelPackage/mac80211/Default)
   TITLE:=Linux 802.11 Wireless Networking Stack
   # +kmod-crypto-cmac is a runtime only dependency of net/mac80211/aes_cmac.c
-  DEPENDS+= +kmod-cfg80211 +kmod-crypto-cmac +kmod-crypto-ccm +kmod-crypto-gcm +hostapd-common
+  DEPENDS+= +kmod-cfg80211 +kmod-crypto-cmac +kmod-crypto-ccm +kmod-crypto-gcm +hostapd-common +PACKAGE_kmod-qca-nss-drv:kmod-qca-nss-drv
   KCONFIG:=\
 	CONFIG_AVERAGE=y
   FILES:= $(PKG_BUILD_DIR)/net/mac80211/mac80211.ko
@@ -141,6 +143,15 @@ endef
 define KernelPackage/mac80211/config
   if PACKAGE_kmod-mac80211
 
+	if PACKAGE_kmod-qca-nss-drv
+	config PACKAGE_MAC80211_NSS_SUPPORT
+		bool "Enable NSS support for IPQ platform"
+		default y
+		help
+		  This option enables support for NSS in boards
+		  like Netgear R7800.
+	endif
+
 	config PACKAGE_MAC80211_DEBUGFS
 		bool "Export mac80211 internals in DebugFS"
 		select KERNEL_DEBUG_FS
@@ -284,7 +295,8 @@ endif
 
 MAKE_OPTS:= \
 	$(subst -C $(LINUX_DIR),-C "$(PKG_BUILD_DIR)",$(KERNEL_MAKEOPTS)) \
-	EXTRA_CFLAGS="-I$(PKG_BUILD_DIR)/include $(IREMAP_CFLAGS) $(C_DEFINES)" \
+	EXTRA_CFLAGS="-I$(PKG_BUILD_DIR)/include $(IREMAP_CFLAGS) $(C_DEFINES) \
+		-I$(STAGING_DIR)/usr/include/qca-nss-drv" \
 	KLIB_BUILD="$(LINUX_DIR)" \
 	MODPROBE=true \
 	KLIB=$(TARGET_MODULES_DIR) \
diff --git a/target/linux/ipq806x/config-6.12 b/target/linux/ipq806x/config-6.12
index 6ade610..b367a35 100644
--- a/target/linux/ipq806x/config-6.12
+++ b/target/linux/ipq806x/config-6.12
@@ -532,3 +532,4 @@ CONFIG_ZLIB_INFLATE=y
 CONFIG_ZSTD_COMMON=y
 CONFIG_ZSTD_COMPRESS=y
 CONFIG_ZSTD_DECOMPRESS=y
+CONFIG_REGULATOR_NSS_VOLT=y
diff --git a/target/linux/ipq806x/files-6.12/arch/arm/boot/dts/qcom/qcom-ipq8068-mr52.dts b/target/linux/ipq806x/files-6.12/arch/arm/boot/dts/qcom/qcom-ipq8068-mr52.dts
index 97b280e..6a6561c 100644
--- a/target/linux/ipq806x/files-6.12/arch/arm/boot/dts/qcom/qcom-ipq8068-mr52.dts
+++ b/target/linux/ipq806x/files-6.12/arch/arm/boot/dts/qcom/qcom-ipq8068-mr52.dts
@@ -76,8 +76,16 @@
 &gmac2 {
 	status = "okay";
 
+	compatible = "qcom,nss-gmac";
 	qcom,id = <2>;
-	mdiobus = <&mdio0>;
+	qcom,pcs-chanid = <2>;
+	qcom,phy-mdio-addr = <0>;
+	qcom,poll-required = <1>;
+	qcom,rgmii-delay = <0>;
+	qcom,forced-speed = <0>;
+	qcom,forced-duplex = <0>;
+	qcom,socver = <0>;
+	mdiobus = <&mdio_gpio0>;
 
 	phy-mode = "sgmii";
 	phy-handle = <&phy0>;
@@ -89,7 +97,15 @@
 &gmac3 {
 	status = "okay";
 
+	compatible = "qcom,nss-gmac";
 	qcom,id = <3>;
+	qcom,pcs-chanid = <3>;
+	qcom,phy-mdio-addr = <4>;
+	qcom,poll-required = <1>;
+	qcom,rgmii-delay = <0>;
+	qcom,forced-speed = <0>;
+	qcom,forced-duplex = <0>;
+	qcom,socver = <0>;
 	mdiobus = <&mdio_gpio0>;
 
 	phy-mode = "sgmii";
